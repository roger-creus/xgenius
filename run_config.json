{
    "mila": {
        "MODULES_TO_LOAD": "{{MODULES}}",
        "SINGULARITY_IMAGE_NAME": "{{IMAGE_NAME}}",
        "OUTPUT_DIR_IN_CONTAINER": "{{OUTPUT_DIR_IN_CONTAINER}}",
        "COMMAND": "{{COMMAND}}",
        "MEM_PER_CPU": "{{MEM_PER_CPU}}",
        "OUTPUT_FILE": "{{OUTPUT_FILE}}",
        "GPUS_PER_TASK": "{{GPUS_PER_TASK}}",
        "PARTITION": "{{PARTITION}}",
        "CPUS_PER_GPU": "{{CPUS_PER_GPU}}",
        "TIME": "{{TIME}}",
        "CODE_DIR": "{{CODE_DIR}}",
        "OUTPUT_DIR_IN_CLUSTER": "{{OUTPUT_DIR_IN_CLUSTER}}"
    }
}